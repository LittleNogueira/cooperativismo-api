server:
    servlet:
        contextPath: /api

spring:
    datasource:
        username: root
        password: root
        url: jdbc:mysql://localhost:3306/banco
    jpa:
        hibernate:
            ddl-auto: update