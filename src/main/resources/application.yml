server:
    servlet:
        contextPath: /api

client:
    user:
        url: https://user-info.herokuapp.com/users

spring:
    datasource:
        username: root
        password: root
        url: jdbc:mysql://localhost:3306/banco
    jpa:
        hibernate:
            ddl-auto: update